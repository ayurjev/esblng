
version: '2'

services:

  geo:
    container_name: geo
    build: .
    env_file:
     - .env
    volumes:
     - .:/var/www

  db:
    container_name: db
    image: registry.dcapi.net/dcpro/main/base/mysql
    env_file:
     - .env
